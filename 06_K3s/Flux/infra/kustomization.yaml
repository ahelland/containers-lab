apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./sources/vikunja-namespace.yaml
  - ./sources/vikunja-repo.yaml

labels:
  - pairs:
      app.kubernetes.io/name: cluster-config
      app.kubernetes.io/managed-by: flux